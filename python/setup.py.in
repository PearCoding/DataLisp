from distutils.core import setup
from distutils.extension import Extension

setup(name='datalisp',
      version='${DL_Version}',
      package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}' },
      packages=['datalisp'],
      package_data={'datalisp': ['pydatalisp.so']})


